interface MySQLProviderOptions {
    host: string | 'localhhost';
    username: string;
    password: string;
    database: string;
    table: string | 'vultrexdb';
}
export default class MySQLProvider {
    private db;
    host: string;
    username: string;
    password: string;
    database: string;
    table: string;
    initialized: boolean;
    constructor(config: MySQLProviderOptions);
    init(): Promise<void>;
    set(key: string | number, value: any): Promise<void>;
    get<T>(key: string | number, defaultValue: any): Promise<any>;
    getAll(key: string | number): Promise<any>;
    size(): Promise<any>;
    delete(key: string | number): Promise<void>;
    clear(): Promise<void>;
}
export {};
